class AppConstants {
  const AppConstants._();

  static const String appName = 'MoneyPilot';
  static const Duration splashDelay = Duration(milliseconds: 900);
  static const Duration relockDelay = Duration(minutes: 3);
}
